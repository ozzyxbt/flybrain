"""Phase two (Gate E): Hyperliquid perpetual trading of independent assets.

Not implemented in Gate A. See docs/claims.md and the build brief: signed
target positions, isolated margin, 1x cap, reduce-only closes, cloid derived
from the decision-envelope hash, never the project token.
"""
